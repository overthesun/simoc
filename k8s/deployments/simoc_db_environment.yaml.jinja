apiVersion: v1
kind: ConfigMap
metadata:
  name: simoc-db-config
  namespace: default
data:
  db_type: 'mysql'
  db_host: '{{ db_host }}'
  db_port: '{{ db_port }}'
  db_name: '{{ db_name }}'
  db_user: '{{ db_user }}'
