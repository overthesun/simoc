name: Deploy SIMOC
on:
  push:
  workflow_dispatch:
    inputs:
      host:
        description: 'Deploy on this host:'
        required: true
        default: ''
      fe_branch:
        description: 'Use this frontend branch:'
        required: true
        default: 'master'
      be_branch:
        description: 'Use this backend branch:'
        required: true
        default: 'master'
      
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Deploy SIMOC
      uses: appleboy/ssh-action@v0.1.10
      env:
        GITHUB_USER: ${{ secrets.SSH_GITHUB_USERNAME }}
        GITHUB_TOKEN: ${{ secrets.SSH_GITHUB_TOKEN }}
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        envs: GITHUB_USER,GITHUB_TOKEN
        script: |
          echo "user: $GITHUB_USER"
          cd simoc-web
          pwd
          git fetch origin
          git reset --hard origin/new-deployment
          # git reset --hard origin/${{ github.event.inputs.fe_branch }}
          git status
          python3 simoc-web.py deploy
          cd ../simoc
          pwd
